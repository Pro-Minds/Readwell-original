Easy-Tricky Questions (1-15)
Which connector type does IDE use?
A) 20-pin
B) 40-pin or 80-pin
C) 24-pin
D) 6-pin
Answer: B) 40-pin or 80-pin
SATA III supports data transfer rates up to:
A) 3 Gbps
B) 6 Gbps
C) 12 Gbps
D) 1 Gbps
Answer: B) 6 Gbps
In Linux, SATA devices are typically named as:
A) /dev/hda
B) /dev/pda
C) /dev/sda
D) /dev/vda
Answer: C) /dev/sda
Which features are unique to SATA compared to IDE? (Multiple correct)
A) Hot-swapping capability
B) Smaller connector
C) Lower speed
D) Parallel data transfer
Answer: A) and B) Hot-swapping capability and Smaller connector
IDE devices in Linux are traditionally named:
A) /dev/sda
B) /dev/hda
C) /dev/ida
D) /dev/dsa
Answer: B) /dev/hda
The first partition on the first SATA drive would be labeled as:
A) /dev/sda
B) /dev/sdb1
C) /dev/sda1
D) /dev/hda1
Answer: C) /dev/sda1
SCSI devices in Linux are represented similarly to:
A) IDE devices
B) SATA devices
C) USB devices
D) Network devices
Answer: B) SATA devices
Which tool is NOT commonly used for partition management in Linux?
A) fdisk
B) parted
C) gparted
D) diskpart
Answer: D) diskpart
What determines the device letter in Linux storage naming?
A) Alphabetical order
B) Detection order
C) Size of device
D) User preference
Answer: B) Detection order
Which filesystem type is native to Linux?
A) NTFS
B) FAT32
C) ext4
D) HFS+
Answer: C) ext4
LVM stands for:
A) Linux Volume Manager
B) Logical Volume Management
C) Large Volume Memory
D) Linear Volume Manager
Answer: B) Logical Volume Management
Which RAID level provides striping without redundancy?
A) RAID 1
B) RAID 0
C) RAID 5
D) RAID 6
Answer: B) RAID 0
What are the primary advantages of SCSI over IDE? (Multiple correct)
A) Higher performance
B) Multiple device connection capability
C) Lower cost
D) Simpler configuration
Answer: A) and B) Higher performance and Multiple device connection capability
The second partition on the first IDE drive would be:
A) /dev/hdb2
B) /dev/sda2
C) /dev/hda2
D) /dev/hd2
Answer: C) /dev/hda2
Which features are NOT associated with SATA? (Multiple correct)
A) 40-pin connector
B) Parallel data transfer
C) Hot-swapping
D) Serial data transfer
Answer: A) and B) 40-pin connector and Parallel data transfer

Medium-Tricky Questions (16-35)
In a system with multiple storage devices, which factors determine the device naming sequence? (Multiple correct)
A) BIOS/UEFI detection order
B) Controller type
C) Port connection order
D) Device manufacturer
Answer: A), B), and C) BIOS/UEFI detection order, Controller type, and Port connection order
What happens when you hot-swap a SATA device in Linux without proper unmounting?
A) Nothing, it's completely safe
B) System automatically repairs any issues
C) Potential data corruption and system instability
D) Device becomes permanently unusable
Answer: C) Potential data corruption and system instability
In LVM, what is the relationship between Physical Volumes (PV) and Volume Groups (VG)?
A) One PV can only belong to one VG, but a VG can contain multiple PVs
B) One PV can belong to multiple VGs
C) VGs must contain exactly two PVs
D) PVs and VGs must be in a 1:1 relationship
Answer: A) One PV can only belong to one VG, but a VG can contain multiple PVs
Which statement about RAID 5 is FALSE?
A) It requires a minimum of three drives
B) It provides parity protection
C) It can survive multiple drive failures simultaneously
D) It distributes parity across all drives
Answer: C) It can survive multiple drive failures simultaneously
What happens when you create a partition that starts at sector 2048? (Multiple correct)
A) Better alignment with physical sectors
B) Improved performance on modern drives
C) Compatibility with older DOS systems
D) Reduced storage capacity
Answer: A) and B) Better alignment with physical sectors and Improved performance on modern drives
Which command combination would you use to safely remove a SCSI device in a running system? (Multiple correct)
A) echo 1 > /sys/block/sdX/device/delete
B) umount /dev/sdX
C) sync
D) scsi-remove /dev/sdX
Answer: A), B), and C
) echo 1 > /sys/block/sdX/device/delete, umount /dev/sdX, and sync
What is the significance of the UUID in storage device identification?
A) It's just a random identifier
B) It's used only for SATA devices
C) It provides a persistent way to identify devices regardless of connection order
D) It's required for RAID configurations only
Answer: C) It provides a persistent way to identify devices regardless of connection order
When using LVM, what happens if a Physical Volume fails? (Multiple correct)
A) Data on other PVs remains accessible
B) The Volume Group becomes completely inaccessible
C) Only Logical Volumes using the failed PV are affected
D) LVM automatically repairs the failure
Answer: A) and C) Data on other PVs remains accessible and Only Logical Volumes using the failed PV are affected
Which SCSI ID has the highest priority on a SCSI bus?
A) ID 0
B) ID 7
C) ID 15
D) ID 1
Answer: B) ID 7
What is the purpose of the 'sync' flag in /etc/fstab? (Multiple correct)
A) Forces immediate writing of data to disk
B) Improves read performance
C) Reduces risk of data loss during power failure
D) Increases disk durability
Answer: A) and C) Forces immediate writing of data to disk and Reduces risk of data loss during power failure

What happens when you use 'write-back' caching on a RAID controller without a battery backup? (Multiple correct)
A) Improved write performance
B) Risk of data corruption during power loss
C) Reduced controller lifespan
D) Increased RAID rebuild times
Answer: A) and B) Improved write performance and Risk of data corruption during power loss
In the context of disk partitioning, what is a cylinder-aligned partition?
A) A partition that starts and ends on physical cylinder boundaries
B) A partition that uses cylindrical recording technology
C) A partition that's optimized for SSDs
D) A partition that spans multiple drives
Answer: A) A partition that starts and ends on physical cylinder boundaries
Which statement about logical volumes is TRUE? (Multiple correct)
A) They can span multiple physical drives
B) They can be resized without unmounting
C) They always provide better performance than physical partitions
D) They can be snapshotted
Answer: A), B), and D) They can span multiple physical drives, They can be resized without unmounting, and They can be snapshotted
What happens when a SATA drive is connected to a port multiplier?
A) The drive becomes read-only
B) Performance may be reduced due to bandwidth sharing
C) The drive must use SATA I speeds
D) Multiple partitions are created automatically
Answer: B) Performance may be reduced due to bandwidth sharing
Which factors affect SCSI device naming in Linux? (Multiple correct)
A) Device type
B) SCSI host adapter order
C) Target ID
D) LUN number
Answer: A), B), C), and D) All options are correct
What is the significance of the 'barrier' mount option?
A) It prevents unauthorized access
B) It ensures write ordering for filesystem integrity
C) It creates a firewall for the device
D) It limits read/write speeds
Answer: B) It ensures write ordering for filesystem integrity
When using multipath I/O, what happens if a path fails?
A) The system crashes
B) I/O continues on alternate paths automatically
C) Manual intervention is required
D) All paths must be reset
Answer: B) I/O continues on alternate paths automatically
Which statement about partition alignment is TRUE? (Multiple correct)
A) Modern drives prefer 4K alignment
B) Misalignment can cause performance issues
C) Alignment only matters for SSDs
D) Alignment affects write amplification
Answer: A), B), and D) Modern drives prefer 4K alignment, Misalignment can cause performance issues, and Alignment affects write amplification
What happens when you exceed the maximum number of partitions on an MBR disk?
A) The disk becomes corrupted
B) You must convert to GPT
C) You can create an extended partition     
D) The system won't boot
Answer: C) You can create an extended partition
Which features are supported by modern SCSI devices? (Multiple correct)
A) Command queuing
B) Hot-plugging
C) Automatic speed negotiation
D) Self-monitoring
Answer: A), B), C), and D) All options are correct

Hard-Tricky Questions (36-65)
When implementing multipath I/O with active/active paths, which scenario would cause path thrashing? (Multiple correct)
A) Different path priorities configured on different hosts
B) Round-robin path selection with varying path latencies
C) Inconsistent ALUA states across storage ports
D) Queue depth mismatch between paths
Answer: A), B), and C) Different path priorities, Round-robin with varying latencies, and Inconsistent ALUA states
In an LVM setup using thin provisioning, what happens when the metadata device becomes full?
A) The system automatically extends the metadata device
B) All I/O to thin pools is suspended
C) Only write operations are blocked
D) The thin pool switches to thick provisioning
Answer: B) All I/O to thin pools is suspended
Which conditions must be met for optimal NCQ (Native Command Queuing) performance? (Multiple correct)
A) AHCI mode enabled in BIOS
B) Queue depth matches drive capabilities
C) Appropriate I/O scheduler selected
D) Interrupts properly balanced across CPUs
Answer: A), B), C), and D) All options are correct
39. What happens during a RAID-5 write hole scenario? (Multiple correct)
    A) Parity becomes inconsistent with data
    B) Partially written stripes exist
    C) Recovery requires full array rebuild
    D) Cache contents are lost
    Answer: A) and B) Parity becomes inconsistent with data and Partially written stripes exist
    When using dm-cache with a fast SSD caching a slower HDD, which scenario would reduce performance? (Multiple correct)
    A) Cache block size smaller than average I/O size
    B) Write-through policy on random writes
    C) Migration threshold set too aggressively
    D) Cache hits causing SSD write amplification
    Answer: A), C), and D) Cache block size too small, Aggressive migration threshold, and Write amplification
    In a system using persistent device naming, which situation could still cause device name changes? (Multiple correct)
    A) Hardware UUID corruption
    B) Duplicate UUIDs after disk cloning
    C) udev rules conflict
    D) Simultaneous device detection
    Answer: A), B), and C) Hardware UUID corruption, Duplicate UUIDs, and udev rules conflict
    What happens when a SCSI device reports a Unit Attention condition? (Multiple correct)
    A) Command queue is flushed
    B) Device state is reset
    C) Host must retry operations
    D) Mode parameters are preserved
    Answer: A), B), and C) Command queue is flushed, Device state is reset, and Host must retry operations
    Which factors affect the choice of RAID stripe size? (Multiple correct)
    A) Typical I/O pattern size
    B) File system block size
    C) Drive rotational latency
    D) Controller cache line size
    Answer: A), B), C), and D) All options are correct
    During an LVM snapshot merge, what happens if the system crashes?
    A) The merge automatically continues after reboot
    B) Both snapshot and original volume are corrupted
    C) The merge must be manually restarted
    D) The snapshot becomes invalid
    Answer: A) The merge automatically continues after reboot
45. Which conditions can cause SCSI command timeouts? (Multiple correct)
    A) SAS expander firmware issues
    B) Excessive command queue depth
    C) Fabric congestion in FC networks
    D) Incorrect multipath settings
    Answer: A), B), C), and D) All options are correct
    In a system using dm-integrity, what happens when a block verification fails? (Multiple correct)
    A) I/O operation returns an error
    B) Block is automatically rewritten from cache
    C) Event is logged to kernel journal
    D) Metadata checksum is recalculated
    Answer: A) and C) I/O operation returns an error and Event is logged to kernel journal
    Which scenarios can cause silent data corruption in a RAID-6 array? (Multiple correct)
    A) Undetected disk sector errors during rebuild
    B) Memory errors in RAID controller cache
    C) Firmware bugs in drive error handling
    D) Power loss during parity update
    Answer: A), B), and C) Undetected disk sector errors, Memory errors, and Firmware bugs
    When using block-level encryption with dm-crypt, what are the implications of using a small sector size? (Multiple correct)
    A) Increased metadata overhead
    B) Better resistance to watermarking attacks
    C) Higher CPU utilization
    D) Reduced storage efficiency
    Answer: A), B), and C) Increased metadata overhead, Better resistance to watermarking attacks, and Higher CPU utilization
    What happens during a TRIM operation on an SSD in these scenarios? (Multiple correct)
    A) Blocks are marked as free in FTL
    B) Garbage collection is triggered immediately
    C) Write performance temporarily decreases
    D) TRIM commands are queued
    Answer: A) and D) Blocks are marked as free in FTL and TRIM commands are queued
50. Which factors affect the choice between hardware and software RAID? (Multiple correct)
    A) CPU overhead for parity calculations
    B) Battery backup availability
    C) Operating system portability requirements
    D) Hot-swap capability requirements
    Answer: A), B), C), and D) All options are correct
    In a system using persistent memory (PMEM), what considerations affect the choice of DAX mode? (Multiple correct)
    A) Application's transaction handling capability
    B) File system support for direct access
    C) Memory bus bandwidth requirements
    D) Cache coherency overhead
    Answer: A), B), C), and D) All options are correct
    What happens when a SCSI reservation conflict occurs? (Multiple correct)
    A) Command is rejected with specific status
    B) Initiator must implement retry logic
    C) Existing reservation is preempted
    D) Device resets reservation state
    Answer: A) and B) Command is rejected with specific status and Initiator must implement retry logic
    Which scenarios can lead to partition misalignment in modern systems? (Multiple correct)
    A) Cloning from different sector size devices
    B) Legacy partitioning tools usage
    C) RAID array reconstruction
    D) Partition table conversion
    Answer: A), B), and D) Cloning from different sector size devices, Legacy partitioning tools usage, and Partition table conversion
    When using multi-queue block layer (blk-mq), what factors affect optimal queue depth? (Multiple correct)
    A) Number of CPU cores
    B) Storage device capabilities
    C) I/O scheduler algorithm
    D) Hardware interrupt distribution
    Answer: A), B), C), and D) All options are correct
    What happens during a T10-DIF validation failure? (Multiple correct)
    A) I/O is failed with specific sense code
    B) Reference tag is recalculated
    C) Error is reported to SCSI mid-layer
    D) Data integrity field is logged
    Answer: A) and C) I/O is failed with specific sense code and Error is reported to SCSI mid-layer
    In a system using NVMe over Fabrics, which conditions can cause controller reconnect issues? (Multiple correct)
    A) Transport layer congestion
    B) Keep-alive timer expiration
    C) Queue pair mismatch
    D) Admin command timeout
    Answer: A), B), C), and D) All options are correct
    When implementing storage QoS with cgroups, what scenarios can lead to priority inversion? (Multiple correct)
    A) Nested cgroup hierarchies
    B) Mixed synchronous and asynchronous I/O
    C) Shared block device queues
    D) Throttle group overlaps
    Answer: A), B), and C) Nested cgroup hierarchies, Mixed I/O types, and Shared block device queues
58. Which factors affect the choice of journal mode in a journaling filesystem? (Multiple correct)
    A) Recovery time requirements
    B) Write pattern characteristics
    C) Power loss protection capability
    D) Memory pressure conditions
    Answer: A), B), C), and D) All options are correct
    During a storage controller failover, what conditions must be met for zero downtime? (Multiple correct)
    A) Consistent SCSI reservation state
    B) Synchronized cache contents
    C) Matching queue depths across paths
    D) Identical controller firmware versions
    Answer: A), B), and D) Consistent SCSI reservation state, Synchronized cache contents, and Identical controller firmware versions
    What happens when a thin-provisioned LUN exceeds its warning threshold? (Multiple correct)
    A) SCSI sense data is generated
    B) Async event notification is sent
    C) Write same commands are rejected
    D) Space reclamation is triggered
    Answer: A) and B) SCSI sense data is generated and Async event notification is sent
    In a system using persistent reservations, which scenarios can cause split-brain? (Multiple correct)
    A) Incomplete SCSI-3 PGR support
    B) Fence device failure
    C) Network partition in cluster
    D) Reservation preemption race
    Answer: A), B), C), and D) All options are correct
    When using zone block devices, what conditions trigger zone reset? (Multiple correct)
    A) Zone becomes full
    B) Explicit management command
    C) Device error recovery
    D) Zone resource exhaustion
    Answer: A), B), and C) Zone becomes full, Explicit management command, and Device error recovery
    Which factors affect the choice of I/O scheduler for NVMe devices? (Multiple correct)
    A) Hardware command reordering capability
    B) Application I/O pattern
    C) CPU scheduling policy
    D) Power management state
    Answer: A), B), and C) Hardware command reordering capability, Application I/O pattern, and CPU scheduling policy
    During storage device firmware update, what conditions must be verified? (Multiple correct)
    A) Quiesced I/O paths
    B) Compatible intermediate versions
    C) Redundant path availability
    D) Recovery procedure readiness
    Answer: A), B), C), and D) All options are correct
    What happens when a storage device reports a predictive failure? (Multiple correct)
    A) SMART attributes are updated
    B) Error recovery procedures activate
    C) Performance may be degraded
    D) Automatic failover is initiated
    Answer: A), B), and C) SMART attributes are updated, Error recovery procedures activate, and Performance may be degraded